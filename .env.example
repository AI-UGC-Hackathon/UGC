# Optional: pre-seed API keys into localStorage on first load.
# Copy this file to .env.local and fill in your keys.
# These values are bundled into the client — never deploy with real keys here.

VITE_GEMINI_KEY=
VITE_ELEVENLABS_KEY=
VITE_EXA_KEY=
VITE_OPENAI_KEY=
VITE_FAL_KEY=
